# Athopia Sync — examples

## Warm Sync (default)

User: `Athopia Sync`

1. `fingerprint.ps1` → dirty = `api` only  
2. Read STATE gaps + L-004  
3. Heal remaining PC gaps on api layer  
4. `test:parity`  
5. Append FINDINGS; refresh IMPROVEMENTS  
6. Report + top 3 improvements  

## Cold Sync

User: `Athopia Sync cold`

Full layer inventory; re-skim constitutions; rewrite STRUCTURE if needed.

## Feature wave

User: `Jag byggde transfer radar på webben`

Cascade web→contracts→iOS only; skip os/admin unless ops needed; document.

## Self-heal from learning

STATE S5 Spelarstats Supabase-direct → match future L-xxx → implement API wrapper → parity → promote learning.

## Efficiency win

Dirty = docs only → skip typecheck/explore → fingerprint → IMPROVEMENTS note “saved typecheck”.
